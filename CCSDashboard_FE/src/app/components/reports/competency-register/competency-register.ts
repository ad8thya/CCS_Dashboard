import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-competency-register',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './competency-register.html',
})
export class CompetencyRegisterComponent {}
