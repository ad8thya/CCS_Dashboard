import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-active-certificates',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './active-certificates.html',
})
export class ActiveCertificatesComponent {}
